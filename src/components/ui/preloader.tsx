"use client";

// Preloader has been disabled to optimize Core Web Vitals (LCP) for conversion
// and to prevent SVG pathLength rendering freezes in Mozilla Firefox.
export function GlobalPreloader() {
  return null;
}
